# Consent service stub

Reserved for a future standalone consent orchestration service.

Note: consent management already exists today as routes inside `services/api` (see `services/api/src/routes/consent.ts`), not here. This directory is for if/when that logic gets split out into its own service.
